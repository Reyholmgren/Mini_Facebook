<h1>Welcome to Fakebook</h1>
<% @users.each do |f| %>
  <ul> 
    <li> 
     <%= link_to f.name, user_path(f) %>
    </li>
  </ul>
<% end %> 

<%= button_to "New User", new_user_path, method: :get %>
